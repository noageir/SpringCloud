# StatViewServlet配置，说明请参考Druid Wiki，配置_StatViewServlet配置
# url:http://localhost:10100/druid/login.html
spring:
  datasource:
    druid:
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        reset-enable: false
        login-username: admin
        login-password: admin
        #allow: 127.0.0.1
        #deny= 192.168.32.139 #访问黑名单
#      web-stat-filter:
#        enabled: true
#        session-stat-max-count: 1000
#        session-stat-enable: false
#        profile-enable: true